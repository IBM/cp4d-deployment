resolversAuth:
  metadata:
    description:  This is the INTERNAL authorization file for certifying a CASE.
  resources:
    cases:
      repositories:     
        cp4dDevIbmGithub:
          credentials:
            basic:
              username: ${git-user}
              password: ${git-token}
    containerImages:
      registries:
        entitledStage:
          credentials:
            basic:
              username: iamapikey
              password: ${STAGING_KEY}