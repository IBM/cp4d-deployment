registry:
    # lite
  - url: hyc-cp4d-team-bootstrap-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    name: base-registry
    # WSL
  - name: base
    url: hyc-cp4d-team-wsl-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    # WKC
  - url: hyc-cp4d-team-wkc-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    name: wkc-registry
    # WML
  - name: wml-base
    url: hyc-cp4d-team-wml-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    # DO
  - name: do-registry
    url: hyc-cp4d-team-do-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    # DB
  - url: hyc-cp4d-team-databases-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    name: databases-registry
    # DV
  - url: hyc-cp4d-team-dv-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    name: dv-registry
    # DB2z
  - url: hyc-cp4d-team-db2z-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    name: db2z-registry
   # CDE
  - url: hyc-cp4d-team-cde-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    name: cde
   # Openscale
  - url: hyc-cp4d-team-openscale-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    name: openscale-registry
   # Streams
  - url: hyc-cp4d-team-streams-docker-local.artifactory.swg-devops.com
    name: streams-registry
    namespace: streams_build
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
   # CA
  - url: hyc-cp4d-team-cognos-analytics-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    name: ca-registry
   # Streams-flows
  - url: hyc-cp4d-team-streams-docker-local.artifactory.swg-devops.com
    name: streams-flows
    namespace: streams_flows
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
  # OSG
  - url: hyc-cp4d-team-osg-docker-local.artifactory.swg-devops.com
    name: osg
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
  # DB2WH
  - url: hyc-cp4d-team-db2wh-docker-local.artifactory.swg-devops.com
    name: db2wh
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
   # SPSS
  - url: hyc-cp4d-team-spss-docker-local.artifactory.swg-devops.com
    name: spss
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
   # PA
  - url: hyc-oddk-team-cp4d-docker-local.artifactory.swg-devops.com
    name: pa-registry
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: "cp/cpd"
   # SPARK
  - url: hyc-ibmae-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    name: spark
   # SPARK-POWER
  - url: hyc-ibmae-power-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    name: spark-power
   #MDC
  - url: hyc-cp4d-team-mdc-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    name: mdc-registry
   #WKC INSTASCAN
  - name: instascan-registry
    url: cp.stg.icr.io/cp/cpd
    username: ${entitlementkey-username}
    apikey: ${entitlementkey}
   #FCAI
  - url: cp.icr.io/cp/cpd
    username: ${entitlementkey-username}
    apikey: ${entitlementkey}
    name: fcai-registry
   #FCI
  - url: cp.icr.io/cp/cpd
    username: ${entitlementkey-username}
    apikey: ${entitlementkey}
    name: fci-registry
   #FCII
  - url: cp.icr.io/cp/cpd
    username: ${entitlementkey-username}
    apikey: ${entitlementkey}
    name: fcii-registry
   #FCDD
  - url: cp.icr.io/cp/cpd
    username: ${entitlementkey-username}
    apikey: ${entitlementkey}
    name: fcdd-registry
   #FCCS
  - url: cp.icr.io/cp/cpd
    username: ${entitlementkey-username}
    apikey: ${entitlementkey}
    name: fccs-registry
   #BIGSQL
  - url: bi-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    name: bigsql-registry
    #SPACES
  - url: hyc-ai-lifecycle-team-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    name: spaces
    #OpenPages
  - url: ip-op-grc-docker-local.artifactory.swg-devops.com/op
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    name: op-registry
    #DP
  - url: hyc-cp4d-tdm-team-docker-local.artifactory.swg-devops.com
    username: ${artifactory-username}
    apikey: ${artifactory-apikey}
    namespace: ""
    name: dp
fileservers:
  - url: http://158.85.173.111/repos/zen/cp4d-builds/3.5.0/promoted/${promoted-build}
